import{L_star}from"../learners/L_star.js";import{HTML_LearnerBase}from"./HTML_LearnerBase.js";export class HTML_L_star extends HTML_LearnerBase{constructor(teacher){super(new L_star(teacher))}close_message(close_rep){let row=this.learner.observation_table[close_rep];return`The table is not closed since row(${close_rep}) is not present in S.
    "${close_rep}" will be moved from SA to S.`}consistent_message(s1,s2,new_col){let fstChar=new_col[0],sndChar=new_col.length==1?"\u03B5":new_col.substring(1);return`The table is not consistent since :
        row(${this.replaceEpsilon(s1)}) = row(${this.replaceEpsilon(s2)}) but row(${s1+new_col[0]}) ≠ row(${s2+new_col[0]});
        The column "${fstChar} ∘ ${sndChar}" will be added in E since T(${this.replaceEpsilon(s1)} ∘ ${new_col}) ≠ T(${this.replaceEpsilon(s2)} ∘ ${new_col})`}}
//# sourceMappingURL=HTML_L_star.js.map